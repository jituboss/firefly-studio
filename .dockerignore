node_modules
.next
.git
.github
coverage
# NOTE: drizzle/meta must NOT be excluded — the migrator reads
# drizzle/meta/_journal.json at runtime to decide which migrations to apply.
*.log
.env
.env.*
!.env.example
# Documentation never needs to be in the runtime image. `docs/` covers the plan,
# the handoff notes, the ADRs and the release guide.
tests/
docs/
README.md
CHANGELOG.md
CONTRIBUTING.md
