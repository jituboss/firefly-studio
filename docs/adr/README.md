# Architecture decision records

One file per decision, numbered sequentially, never edited once accepted —
superseded records get a new ADR that references them.

Format: Context → Decision → Consequences. Keep them short; a record nobody
reads is worse than none.
